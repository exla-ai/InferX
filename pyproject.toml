[project]
name = "exla"
version = "0.1.0"
description = "Exla SDK for model optimization for edge"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.5.1", 
    "ultralytics=*",
    "opencv-python=*"
]

[tool.setuptools]
packages = ["exla"]
