[project]
name = "exla"
version = "0.1.0"
description = "Exla SDK for model optimization for edge"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "clip-client>=0.8.3",
    "clip-server>=0.8.3",
    "pillow>=11.1.0",
]

[tool.setuptools]
packages = ["exla"]
